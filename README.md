# URL Shortener API

Этот проект представляет собой простое веб-приложение на Python, реализующее API для сокращения URL-адресов. Приложение позволяет пользователям передать длинную ссылку и получить короткий, уникальный URL, который при открытии перенаправляет на оригинальный адрес.

---

## Возможности

- **Сокращение URL**: Отправьте `POST`-запрос с длинной ссылкой и получите короткий URL.
- **Редирект**: Откройте короткий URL в браузере, и вас автоматически перенаправит на оригинальный адрес.
- **Валидация URL**: Приложение проверяет, является ли переданный URL действительным.
- **Логирование**: Все события API (создание ссылки, редирект, ошибки) логируются.
- **Тестирование**: Проект включает интеграционные тесты с использованием `pytest`.

---

## 🛠️ Технологии

- **Python 3.12+**
- **FastAPI** – современный, быстрый (высокая производительность) веб-фреймворк для создания API.
- **SQLite3** – легковесная встраиваемая система управления базами данных.
- **Pydantic** – для валидации данных запросов и ответов.
- **Pytest** – для написания и запуска тестов.

---

## Установка и запуск

1.  **Клонируйте репозиторий:**

    ```bash
    git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
    cd <НАЗВАНИЕ_ПРОЕКТА>
    ```

2.  **Создайте и активируйте виртуальное окружение (рекомендуется):**

    ```bash
    python -m venv venv
    source venv/bin/activate  # Linux/macOS
    # или
    # venv\Scripts\activate   # Windows
    ```

3.  **Установите зависимости:**

    ```bash
    pip install -e .
    ```
    Команда `pip install -e .` установит проект и его зависимости, указанные в `pyproject.toml`.

4.  **Запустите приложение:**

    ```bash
    start
    ```
    Или, если команда `start` не работает:
    ```bash
    uvicorn main:app --reload
    ```
    Приложение будет доступно по адресу `http://localhost:8000`.

---

## Тестирование

Для запуска тестов выполните:

```bash
run-tests
```

## API Endpoints
Приложение предоставляет следующие эндпоинты:
### Сократить URL
* URL: `POST /shorten`
* Описание: Принимает JSON-объект с полем `url` и возвращает JSON с сокращённым URL.
* Пример запроса:
```json
{
    "url": "https://www.example.com/very/long/path/to/resource"
}
```
* Пример ответа:
```json
{
    "short_url": "http://localhost:8000/a1b2c3d4"
}
```
### Редирект по коду
* URL: `GET /{code}`
* Описание: Перенаправляет (HTTP 307 Temporary Redirect) на оригинальный URL, связанный с указанным `{code}`.
* Пример запроса: `GET /a1b2c3d4`
* Ответ: HTTP 307 с заголовком `Location`, указывающим на оригинальный URL.
## 📁 Структура проекта
```
<НАЗВАНИЕ_ПРОЕКТА>/
│
├── main.py                 # Точка входа в приложение FastAPI
├── pyproject.toml          # Конфигурация проекта, зависимости
├── README.md               # Этот файл
├── database.py             # Логика работы с базой данных (SQLite)
├── schemas.py              # Модели данных Pydantic
├── utils.py                # Вспомогательные функции (валидация, генерация кода)
├── logging_config.py       # Настройки логирования
├── routers/
│   └── urls.py             # Роутеры FastAPI (эндпоинты)
└── tests/
    └── test_app.py         # Интеграционные тесты
```